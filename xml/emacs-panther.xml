<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE page PUBLIC "-//SWEETOHM//DTD page 1.0//EN"
                      "../dtd/page.dtd">
<page author="Michel CASABIANCA"
      email="casa@sweetohm.net"
      date="2004-01-06"
      id="emacs-panther"
      lang="fr">

  <title>Emacs sous Panther</title>

  <text>

    <p>Jusqu'à très récemment, il était impossible, pour nous autres
    français, d'installer une version d'Emacs gérant les caractères
    accentués sous MacOSX. C'est maintenant du passé et je présente dans
    cette page les liens et la procédure pour installer Emacs avec le
    support de l'encodage ISO-8859-1.</p>

    <p>Il faut commencer par télécharger un build d'Emacs pour
    Panther. On le trouvera à l'adresse <link
    url="http://www.mindlube.com/products/emacs/"/>. L'archive est une
    image disque qui devrait être montée automatiquement sur votre
    bureau par votre navigateur. Il suffit alors de déplacer
    l'application dans votre répertoire <file>/Applications</file>.</p>

    <p>Il faut ensuite télécharger les fontes appropriées. On pourra
    les trouver à l'adresse <link
    url="http://members.shaw.ca/akochoi-emacs/stories/Resources/GNU-Fonts.smi.bin"/>.
    Cette archive est aussi une image disque et il suffit de déplacer
    les fontes qu'elle contient dans votre répertoire
    <file>~/Library/Fonts</file> (ou encore dans
    <file>/Library/Fonts</file> pour en faire profiter tous les
    utilisateurs de votre Mac).</p>

    <p>Pour finir, il faut encore indiquer à Emacs la méthode de saisie
    du texte (pour être capable de saisir des caractères en
    ISO-8859-1). Pour ce faire, on ajoutera la ligne suivante dans son
    <file>.emacs</file> :</p>
    
    <source>(setq mac-keyboard-text-encoding kTextEncodingISOLatin1)</source>

    <p>Vous pouvez maintenant lancer Emacs et profiter des caractères
    accentués :</p>

    <figure url="emacs-panther.screenshot.png">
      <title>Un bien bel Emacs...</title>
    </figure>

    <p>L'Emacs ainsi installé est aussi celui qui sera lancé dans le
    terminal et remplacera avantageusement celui livré avec l'OS, en
    apportant la colorisation syntaxique et une meilleure gestion du
    clavier. On pourra ajouter les lignes suivantes dans son
    <file>.emacs</file> pour enlever la ligne de menu, inutile en mode
    terminal :</p>

    <source>(if (eq window-system nil)
    ;; Terminal
    (progn
      (menu-bar-mode nil)
      )
  ;; Aqua
  (progn 
    (setq mac-keyboard-text-encoding kTextEncodingISOLatin1)
    (set-screen-width 93)
    (set-screen-height 60)
    (set-cursor-color "red")
    ))</source>

    <figure url="emacs-panther.terminal.png">
      <title>Cette version d'Emacs ajoute plein de bonnes choses dans le
      terminal</title>
    </figure>

    <p>La colorisation en mode terminal nécessite un terminal
    supportant les codes couleur. Pour ce faire, les utilisateurs de
    iTerm (un terminal évolué pour MacOSX) pourront ouvrir la boîte de
    dialogue des préférences (en cliquant sur
    <imp>iTerm/Préférences</imp>), puis en cliquant sur le bouton
    <imp>Session par défaut</imp> et en sélectionnant l'item
    <imp>xterm-color</imp> dans la liste déroulante.</p>

    <figure url="emacs-panther.iterm-prefs.png">
      <title>Préférences de iTerm pour profiter de la colorisation
      dans le terminal</title>
    </figure>

    <p>Pour finir, je trouve pour ma part la taille de la police par
    défaut trop grande et j'ai trouvé le moyen de la réduire avec la
    ligne suivante dans mon <file>.emacs</file>&dp;</p>

    <source>(set-default-font "-etl-fixed-medium-r-normal--12-120-75-75-m-120-iso8859-1")</source>

    <p>Cependant, ce paramétrage n'est pas parfait car il ne change
    pas la taille de toutes les fontes utilisées par Emacs (par
    exemple, la fonte utilisée par la speedbar reste inchangée). Toute
    amélioration bienvenue&pe;</p>

    <p>Enjoy!</p>

  </text>

</page>

<!-- Local IspellDict: francais -->
